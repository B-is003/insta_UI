import 'package:flutter/material.dart';

class UserLikes extends StatelessWidget {
  const UserLikes({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(child: Text('LIKES')),
    );
  }
}
